# Грамматика и правила языка Udav

## Содержание







## Функция

Функция - это именованная, лексически замкнутая вычислительная абстракция,
определяющая отображение из упорядоченной последовательности входных значений (параметров)
в результат вычисления и/или побочные эффекты \

### Имена функций

Каждая функция должна обладать именем

Каждое имя функции - уникально и не может быть переиспользовано 
для объявления другой функции повторно. Результат - ошибка выполнения

Имя функции используется для того, чтобы потом обратиться к ней для вызова

```udav
fun foo():
    ...
    
fun foo(a): # ошибка выполнения
    ...

```

#### Зарезервированные имена функций

В языке существуют зарезервированные имена функций, которые нельзя использовать 
при написании кода. Попытка - ошибка выполнения

К ним относятся:
1) Встроенные функции работы с типами (`int`, `bool`, `substr` и пр.)
2) Имена инструкций по типу (`let`, `continue` и пр.)
3) Имя `main`

### Объявление функции

Функция объявляется ключевым словом fun, далее идёт имя и набор параметров в скобках

Пример объявления функции
```udav
# Здесь foo - имя фукнции
# a, b, c - список параметров
fun foo(a, b, c):
    ...
```

### Параметры функции

Параметры функции - это упорядоченная последовательность именованных переменных, 
объявляемых в определении функции и предназначенных для получения значений аргументов при её вызове

Их количество может быть произвольным

## Основная программа

Основная программа в языке начинается с функции с именем `main`


В программе не должно быть более или менее 1 функции `main`. Иначе - ошибка выполнения

```udav
func main():
    ...
```

```udav
func not_main(): # ошибка выполнения - нет функции main
    ...
```

```udav
func main():
    ...
    
func main(): # ошибка выполения - повторно объявленная функция main
    ...
```

## Инструкции

### Инструкции на уровне [program](grammar.ebnf)

Допустимые инструкции на уровне `program`
- Объявление функции

Если на уровне `program` находятся прочие инструкции - ошибка выполнения

**Пример:**

```Udav
let c = 3 # ошибка: на этом уровне можно только объявлять переменные

fun foo(): # может быть объявлена
    pass 

fun main():
    let a = 3 
    fun bar(): # ошибка: язык не поддерживает вложенные функции
        print(a) 
```

#### Инструкции на уровне [block](grammar.ebnf)

Допустимые инструкции на уровне `block`
- Объявление переменной.
- Оператор присваивания.
- Вызов функции
- `if`.
- `while`.
- `println`|`print`.
- `break`|`continue`
- `pass`

Если на уровне `block` находятся прочие инструкции - ошибка выполнения

**Пример:**

```Udav
fun main():
    let a = 3 
    a = 5
    println(a)
    if a > 0:
        println("Positive")
    while a < 10:
        a = a + 1
    let b = readln()
```

### Объявление переменной

#### Объявление с инициализацией

В Udav каждая переменная должна быть объявлена с начальным значением\
*Если переменная не проинициализирован при объявлении, то выполнение завершается ошибкой выполнения*

```Udav
let i = 1
let i = "string"
let a # ошибка выполнения
```

#### Множественное объявление

В Udav поддерживается множественное объявление переменных с типами,
определёнными на основе значений. В одной строке можно объявить несколько переменных.

```Udav
let a = 1, b = "string", c = 3
```

#### Повторное объявление

Повторное объявление переменной запрещено. Результат - ошибка выполнения

```udav
let a = 3
let a = 2 # ошибка выполнения
```

### Присвоение переменной
    - операторы присваивания
    - использование в выражении
    - составное присвоение

### вызов функции
    - вызов функции как expression
    - значение теряется в любом случае
    - let <anonymous variable> = f()
    - встроенные функции (print, println)
### управление потоком
    - циклы
      - break
      - continue
      - о том, как работают
    - условия
      - выполняется только 1 ветвь
### pass
### return
    - ранний выход из функции
    - return необязателен, по умолчанию null
    - неявный return
    - просто `return` === `return null`

TODO ввести определения
TODO добавить содержание
TODO у нас длинная арифметика, bigint
TODO определения ошибок в процессе выполнения программы

let a = 3, b = "4"
a *= 3 *= 4





## Правила объявления переменных



### Оператор присваивания

Оператор присваивания используется для назначения значения переменной.
Присваивание может быть как прямым, так и комбинированным (например, присваивание с операцией)

```Udav
i = 2
i = "another string"
```

## Инструкции ввода-вывода

### Вывод в консоль

Для вывода в консоль используются встроенные функции:

```Udav
print(...)
println(...)
```

`...` означает, что на вход подаётся произвольное количество параметров. Обе функции выводят значения, разделяя их
пробелом.

**Примеры:**

```Udav
print()  # Печать пустой строки
print(a, 1, "string", foo())  # Печать нескольких значений
```

### Ввод из консоли

Для ввода данных из консоли используется функция:

```Udav
readln()
```

Функция `readln()` читает данные и возвращает значение типа string.
**Пример:**

```Udav
let a = readln()
```

## Приведения типов

Учитывая, что функция `readln()` всегда возвращает строку, в языке будут встроенные функции для приведения типов

```Udav
let b = "123"
let a = int(b) # тип а - число
let c = string(a)
```

## Правила в формате EBNF

```Udav
program =
    [ newline ] , { function } , EOF ;

function =
    "fun" , SYMBOL , "(" , [ argument_list ] , ")" , ":" , block ;

argument_list =
    SYMBOL , { "," , SYMBOL } ;

block =
    newline , INDENT , { stmt } , DEDENT ;

stmt =
    let_stmt |
    assign_stmt |
    pass_stmt |
    continue_stmt |
    break_stmt |
    return_stmt |
    call_stmt |
    if_stmt |
    while_stmt ;

let_stmt =
    "let" , variable_decl , { "," , variable_decl } , newline ;

variable_decl =
    SYMBOL , "=" , expr ;

assign_stmt =
    SYMBOL , assign_op , expr , newline ;

assign_op =
    "=" |
    "+=" |
    "-=" |
    "*=" |
    "/=" |
    "%=" |
    "**=" |
    "|=" |
    "&=" |
    "^=" |
    ">>=" |
    "<<=" ;

pass_stmt =
    "pass" , newline ;

continue_stmt =
    "continue" , newline ;

break_stmt =
    "break" , newline ;

return_stmt =
    "return" , [ expr ] , newline ;

call_stmt =
    SYMBOL , "(" , [ expr_list ] , ")" , newline ;

if_stmt =
    "if" , expr , ":" , block ,
    { "elif" , expr , ":" , block }
    [ "else" , ":" , block ] ;

while_stmt =
    "while" expr ":" block ;

expr_list =
    expr , { "," , expr } ;
```

## Пример кода

```udav
fun nothing():
    pass
    return

fun all_features(a, b):
    let x = 1, y = 2, z = 3
    let s = "hello", t = "world"
    let flag = true, flag2 = false

    x = 10
    x += 1

    let neg = -x
    let eq = a == b
    let bits = (a | b) ^ (a & b)
    let pow = a ** b ** 2
    let call_expr = max(a, b, x + y)

    if a > b:
        println("a > b")
    elif a == b:
        println("a == b")
    else:
        println("a < b")

    while true && flag:
        bits += 1

        if bits & 1 == 0:
            println("even")
            continue

        if bits > 100:
            println("stop")
            break

        println(bits)
    return bits

fun main():
    let a = 3, b = 5
    let result = all_features(a, b)
    println(result)
```
